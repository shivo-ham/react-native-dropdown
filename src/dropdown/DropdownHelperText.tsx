import React from 'react';
import { View } from 'react-native';
interface DropdownHelperTextProps {
}

const DropdownHelperText: React.FC<DropdownHelperTextProps> = ({
}) => {

    return (
        <View>
        </View>
    );
};
export default DropdownHelperText;